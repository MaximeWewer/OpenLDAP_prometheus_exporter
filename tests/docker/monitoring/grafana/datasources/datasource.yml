apiVersion: 1

datasources:
  - name: Prometheus
    # uid must match the dashboard's ${datasource} template variable value
    # ("Prometheus") so panels resolve without manual datasource picking.
    uid: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
